meta {
  name: Update Reply
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}{{apiPrefix}}/forum/replies/{{replyId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "content": "Édit : précision ajoutée — formats supportés : glb, gltf, obj, fbx, stl."
  }
}

tests {
  test("returns 200 or 403", function() {
    expect([200, 403]).to.include(res.getStatus());
  });
}

docs {
  Édite le contenu d'une réponse. Auteur OU staff.
}
