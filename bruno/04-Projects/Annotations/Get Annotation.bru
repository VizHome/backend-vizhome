meta {
  name: Get Annotation
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}{{apiPrefix}}/projects/{{projectId}}/annotations/{{annotationId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

docs {
  Récupère une annotation individuelle.
}
